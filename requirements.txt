# Pinned, top-level runtime dependencies for the full pipeline.
# For just the pure-Python core, only the first block is required.
#
# Core (pure-numeric modules: weights, panel, indirect water, TE benchmark)
pandas>=2.0
numpy>=1.24
pyyaml>=6.0

# Geospatial (spatial units, weights from geometry, maps)
geopandas>=0.14
shapely>=2.0

# Econometrics
statsmodels>=0.14
linearmodels>=5.3      # PanelOLS (DiD, event study)
libpysal>=4.9          # contiguity / kNN / kernel spatial weights
spreg>=1.4             # SAR / SDM maximum-likelihood estimation

# Visualization & I/O
matplotlib>=3.7
seaborn>=0.12
openpyxl>=3.1          # multi-sheet Excel outputs

# Dev
pytest>=7.4
